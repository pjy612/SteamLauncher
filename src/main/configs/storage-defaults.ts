const storageDefault: StoreType = {
  settings: {
    network: true,
    httpsRejectUnauthorized: true,
    minimizeToTray: true,
    minimizeToTrayWhenLaunchGame: true,
    ssePersist: false,
    sseInjectDll: false,
    sseParanoidMode: false,
    emulatorUpdater: true,
  },
  window: {
    bounds: undefined,
    isFullScreen: false,
    isMaximized: false,
  },
};

export default storageDefault;
